/**
 * TUI Router
 *
 * Simple router for navigating between views in the TUI.
 */

import React from 'react'
import type { AppDefinition } from '@saaskit/core'

interface RouterProps {
  app: AppDefinition
}

export function Router({ app }: RouterProps) {
  // TODO: Implement actual routing based on app routes
  return (
    <></>
  )
}
