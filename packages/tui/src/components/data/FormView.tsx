/**
 * FormView Component
 *
 * Renders a form for input in the TUI.
 */

import React from 'react'
import type { FormView as FormViewType } from '@saaskit/core'

interface FormViewProps {
  view: FormViewType
}

export function FormView({ view }: FormViewProps) {
  // TODO: Implement FormView rendering
  return null
}
