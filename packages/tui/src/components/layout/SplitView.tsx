/**
 * SplitView Component
 *
 * Renders a split pane layout in the TUI.
 */

import React from 'react'
import type { SplitView as SplitViewType } from '@saaskit/core'

interface SplitViewProps {
  view: SplitViewType
}

export function SplitView({ view }: SplitViewProps) {
  // TODO: Implement SplitView rendering
  return null
}
